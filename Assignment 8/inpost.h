#ifndef INPOST_H
  #define INPOST_H
#include <string>
using namespace std;

string convert(const string&);

bool testForOp(char);

int pemdas(char);//tests operator for precedence

#endif
